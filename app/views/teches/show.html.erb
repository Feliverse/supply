<p style="color: green"><%= notice %></p>
<div style="max-width: 30rem;">
<%= render @tech %>

<div class="d-flex justify-content-between align-items-center mt-5">
  <div class="btn-group">
  <%= link_to edit_category_subcategory_marca_tech_path(@tech.marca, @tech), class: "btn btn-primary" do%> 
    <i class="fas fa-edit"></i> Editar
  <% end %>
  <%= link_to category_subcategory_marca_teches_path(@tech.marca), class: "btn btn-secondary" do%>
    <i class="fas fa-arrow-left"></i> Volver
  <% end %>
  </div>  
  <%= button_to "Destroy this tech", [@tech.marca, @tech], method: :delete %>
</div>
</div>