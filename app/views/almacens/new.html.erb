<div class="container-fluid">
  <div class="row">
    <%= render 'nav' %>
    <main class="col-md-9 ml-sm-auto col-lg-10 px-md-4">
      <div class="card mt-4">
        <div class="card-header bg-primary text-white">
          <h1 class="mb-0">Nueva Sucursal</h1>
        </div>
        <div class="card-body">
          <%= render "form", almacen: @almacen %>
          <div class="mt-3">
            <%= link_to "Atrás", almacens_path, class: "btn btn-secondary" %>
          </div>
        </div>
      </div>
    </main>
  </div>
</div>
